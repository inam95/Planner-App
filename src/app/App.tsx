import React from "react";
import Homepage from "./containers/home/pages/Homepage";

interface AppProps {}

const App = (props: AppProps) => {
  return (
    <div>
      <Homepage />
    </div>
  );
};

export default App;
